# Garbage Panel Project

This is a C++ project called "Garbage Panel".

## Code style directions
- Always use strings prefixed with `u8` or `L`
- When working with files or explicitly guided to work with Cyrillic, use wide classes (wcout, L"strings", etc.)
- All display labels must be in Russian.

## Building 
Use `g++`.